<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="false"
        android:configChanges="locale"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Calmscient"
        tools:targetApi="33">
        <activity
            android:name=".activities.UserMoodActivity"
            android:exported="false"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".activities.SplashScreenActivity"
            android:configChanges="locale"
            android:exported="true"
            android:theme="@style/Theme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />
        <activity
            android:name=".activities.DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".activities.WeeklySummary"
            android:exported="false"
            android:label="WeeklySummary Activity"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/Theme.NoActionBar" />
        <activity android:name=".base.BaseActivity" />
        <activity
            android:name=".activities.MedicationsActivity"
            android:exported="false"
            android:label="MedicationsActivity"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".activities.CalendarViewActivity"
            android:exported="false"
            android:label="CalendarActivity"
            android:theme="@style/Theme.NoActionBar" />
    </application>
</manifest>